{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
    <h1>Book</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ book.name }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ book.author }}</td>
            </tr>
            <tr>
                <th>Cover</th>
                <td><img src="{{ book.cover}}" style='height: 200px;'></td>
            </tr>
            <tr>
                <td><a href="{{ book.file }}" download>Скачать книгу</a></td>
            </tr>
            <tr>
                <td><a href="{{ book.file }}">Читать книгу</a></td>
            </tr>
            <tr>
                <th>Last_reading_date</th>
                <td>{{ book.lastReadingDate ? book.lastReadingDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_index') }}">back to list</a>

    <a href="{{ path('app_book_edit', {'id': book.id}) }}">edit</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
